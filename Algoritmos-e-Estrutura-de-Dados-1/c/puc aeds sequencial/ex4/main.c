#include <stdio.h>
#include <stdlib.h>
int main()
{
    float x, y, z, a, m;
    scanf ("%f %f %f f", &x, &y, &z, &a);
    m = (x*1 + y*2 + z*3 + a*4)/10;

    printf("media ponderada: %.2f\n", m);

    return 0;
}
