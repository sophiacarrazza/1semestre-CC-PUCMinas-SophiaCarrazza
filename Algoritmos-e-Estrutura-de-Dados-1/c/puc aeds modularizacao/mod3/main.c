#include <stdio.h>
#include <stdlib.h>

int crescente (int val[], int n) {
    min= val[0];

    for (i=0; i < n; i++) {
        for (i=0; i < 3; i++) {
                if (val[i] <= min )

    }
    }


}

int main()
{
    int n;
    printf("Digite a quantidade de conjuntos que voce quer saber a ordem\n");
    scanf("%d", n);

    int val[n];

    for (int i = 0; i < n ; i++) {

    printf("Digite os tres numeros do conjunto %d:\n", i);
    for (i=0; i < 3; i++) {
         scanf("%d", val[i]);
    }


    }


    return 0;
}
